# Архитектура системы BIM-автоматизации

## Общая схема

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              ИСТОЧНИКИ ДАННЫХ                               │
│                                                                             │
│  ┌─────────────────┐      ┌─────────────────┐                             │
│  │  Autodesk Revit │      │ Autodesk        │  Отметка элементов как       │
│  │     2025        │      │ Navisworks      │  забетонированных            │
│  │  (модель,       │      │ (статус         │  (в ходе контроля работ)    │
│  │   параметры)    │      │  выполнения)    │                             │
│  └────────┬────────┘      └────────┬────────┘                             │
│           │                        │                                       │
│           └────────┬─────────────────┘                                       │
│                    │ Экспорт через плагин Signal                           │
│                    ▼                                                        │
│  ┌─────────────────┐                                                        │
│  │  Signal (sgnl)  │  Платформа управления данными BIM                      │
│  │     API         │  Хранение, версионирование, доступ по API              │
│  └────────┬────────┘                                                        │
└───────────┼─────────────────────────────────────────────────────────────────┘
            │ HTTPS / JSON
            ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           ОРКЕСТРАТОР (n8n)                                 │
│                                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│  │   Trigger   │───▶│  Get Token  │───▶│ DOCS_CSV    │───▶│  Download   │  │
│  │ (Telegram/  │    │  (OAuth2)   │    │  (params)   │    │  File       │  │
│  │  Webhook)   │    └─────────────┘    └─────────────┘    └──────┬──────┘  │
│  └─────────────┘                                                  │         │
│                                                                   ▼         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│  │  Extract    │◀───│ HTTP Request│◀───│  Parse &    │◀───│  Sort CSV   │  │
│  │  from File  │    │ (signedUrl) │    │  Sort CSV   │    │  by Date    │  │
│  └──────┬──────┘    └─────────────┘    └─────────────┘    └─────────────┘  │
│         │                                                                   │
│         ▼                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    ПАРАЛЛЕЛЬНАЯ ЗАПИСЬ                              │   │
│  │                                                                     │   │
│  │   ┌─────────────────────┐        ┌─────────────────────┐           │   │
│  │   │ Журнал бетонных     │        │ Журнал ухода за     │           │   │
│  │   │ работ (Google       │        │ бетоном (Google     │           │   │
│  │   │ Sheets)             │        │ Sheets)             │           │   │
│  │   │ appendOrUpdate      │        │ appendOrUpdate      │           │   │
│  │   └──────────┬──────────┘        └──────────┬──────────┘           │   │
│  │              │                              │                       │   │
│  │              ▼                              ▼                       │   │
│  │   ┌─────────────────────┐        ┌─────────────────────┐           │   │
│  │   │  Telegram Notify    │        │  Telegram Notify    │           │   │
│  │   │  "Обновлено: N      │        │  "Обновлено: N      │           │   │
│  │   │   записей"          │        │   записей"          │           │   │
│  │   └─────────────────────┘        └─────────────────────┘           │   │
│  │                                                                     │   │
│  │   ┌─────────────────────────────────────────────────────────────┐   │   │
│  │   │ Execute Command: python google_sheet_to_excel.py --project  │   │   │
│  │   │                    Экспорт в Excel для архива               │   │   │
│  │   └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            ПОТРЕБИТЕЛИ                                      │
│                                                                             │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐         │
│  │  Google Sheets  │    │   Telegram      │    │  Excel-файлы    │         │
│  │  (онлайн-       │    │   (уведомления  │    │  (локальный     │         │
│  │   доступ)       │    │    для команды) │    │   архив)        │         │
│  └────────┬────────┘    └─────────────────┘    └────────┬────────┘         │
│           │                                             │                   │
│           │              ┌─────────────────┐            │                   │
│           └─────────────▶│   Signal Docs   │◀───────────┘                   │
│                          │   (СОД — среда  │                                │
│                          │    общих данных)│                                │
│                          │   Доступ для    │                                │
│                          │   ПТО, прорабов,│                                │
│                          │   инженеров     │                                │
│                          └─────────────────┘                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Поток данных (последовательность)

```
1. Пользователь отправляет команду /update_journal в Telegram
   ↓
2. n8n получает webhook от Telegram Bot
   ↓
3. Узел DOCS_CSV задаёт параметры: проект, папка, имя файла
   ↓
4. DownloadFileFromDocs (Code node):
   4.1. Авторизация в Signal API (clientId + clientSecret + scopes)
   4.2. Поиск проекта по имени
   4.3. Рекурсивный обход папок (folder_name может содержать путь)
   4.4. Получение itemId → versionId → signedUrl
   4.5. Скачивание CSV-файла
   ↓
5. Extract from File: парсинг CSV (разделитель ;, UTF-8 с BOM)
   ↓
6. Parse & Sort CSV (Code node):
   6.1. Очистка ключей от переносов строк
   6.2. Сортировка по дате (формат ДД.ММ.ГГГГ)
   ↓
7. Параллельная запись в две Google-таблицы:
   7.1. Журнал бетонных работ (appendOrUpdate по наименованию конструкции)
   7.2. Журнал ухода за бетоном (appendOrUpdate по наименованию части)
   ↓
8. Параллельные уведомления в Telegram
   ↓
9. Execute Command: запуск Python-скрипта для экспорта Google Sheets → Excel
10. Загрузка Excel-файлов в Signal Docs (СОД) для доступа ПТО и всех участников проекта
```

## Структура данных (маппинг CSV → Google Sheets)

### Журнал бетонных работ

| Колонка Google Sheets | Источник в CSV | Примечание |
|----------------------|----------------|------------|
| Дата и время укладки | `SIGNAL Completed` | Формат ДД.ММ.ГГГГ |
| Наименование конструкции | `Объект Имя` + `Маркер` + `Отметки` + `Комментарии` + `ADSK_Этаж` | Конкатенация полей |
| Изготовитель | `SIGNAL Contractor` | — |
| Номер партии | `SIGNAL Номер партии` | — |
| Объём, м³ | `Объект Объем` | — |
| Температура воздуха | `SIGNAL Температура` | — |
| Способ твердения | Константа | «Естеств. условия» |
| Класс прочности | `Тип в приложении Revit Описание` | — |
| % прочности | `SIGNAL процент` | — |
| Подпись | Константа | «Чепкасов С. А.» |

### Журнал ухода за бетоном

| Колонка Google Sheets | Источник в CSV | Примечание |
|----------------------|----------------|------------|
| Наименование части | `Объект Имя` + `Маркер` + `Отметки` + `Комментарии` | Конкатенация |
| Дата укладки | `SIGNAL Completed` | — |
| Объём бетона | `Объект Объем` | — |
| Метод выдерживания | Константа | «Естеств. условия» |
| Температура бетона | `SIGNAL Температура бетона` | — |
| Температура воздуха | `SIGNAL Температура` | — |
| Дата/время начала | `SIGNAL Completed` + `SIGNAL Время` | Конкатенация |
| Продолжительность | Константа | «3 сут.» |
| % теор. прочности | `SIGNAL процент` | — |
| Ф.И.О. ответственного | Константа | «Чепкасов С. А.» |

## Экспорт в Signal Docs (СОД)

После обновления Google-таблиц запускается Python-скрипт:

```bash
/home/node/.n8n/venv/bin/python /home/node/.n8n/scripts/google_sheet_to_excel.py --project 6.2
```

**Что происходит:**
1. Скрипт подключается к Google Sheets API
2. Экспортирует данные из «Журнала бетонных работ» и «Журнала ухода за бетоном»
3. Форматирует в Excel с правильными стилями, шириной колонок, закреплением заголовков
4. Сохраняет файлы локально
5. Файлы загружаются в **Signal Docs** — среду общих данных (СОД)

**Зачем это нужно:**
- **ПТО** и прорабы работают в Signal Docs, а не в Google Sheets
- **Единое хранилище** — все документы проекта в одном месте
- **Версионирование** — история изменений журналов
- **Контроль доступа** — роли и права через Signal
- **Не нужен доступ к Google** — участники проекта скачивают Excel напрямую из СОД

## Технологический стек

| Слой | Технология | Назначение |
|------|-----------|------------|
| BIM-модель | Autodesk Revit 2025 | Источник геометрии и параметров |
| BIM-координация | Autodesk Navisworks | Отметка выполненных работ, статус бетонирования |
| Data Platform | Signal (sgnl.pro) | Хранение и API-доступ к данным |
| Integration | n8n (self-hosted) | Оркестрация, ETL, автоматизация |
| Storage | Google Sheets API v4 | Онлайн-таблицы для команды |
| Notifications | Telegram Bot API | Мгновенные уведомления |
| Export | Python 3 + openpyxl | Локальный экспорт в Excel |
| Auth | OAuth2 / JWT | Безопасный доступ к API |

## Особенности реализации

### Рекурсивный обход папок
```javascript
// Поддержка путей: "Папка1/Папка2/Папка3" или "Папка1\\Папка2\\Папка3"
const splitPath = (p) =>
  normalize(p)
    .replaceAll('\\', '/')
    .split('/')
    .map(x => x.trim())
    .filter(Boolean);
```

### Регистронезависимый поиск
```javascript
const sameName = (a, b) => 
  normalize(a).toLowerCase() === normalize(b).toLowerCase();
```

### Два варианта задания пути
1. `folder_name = "6. СМР/Журнал бетонных работ"`, `file_name = "12.csv"`
2. `folder_name = ""`, `file_name = "6. СМР/Журнал бетонных работ/12.csv"`

### AppendOrUpdate (upsert)
- Ключ поиска: наименование конструкции
- Если запись существует — обновляется
- Если нет — добавляется новая строка
- Предотвращает дублирование при повторных запусках
